import React from 'react';
import {View, Text} from 'react-native';

const PostEvent = () => {
    return (
        <View>
            <Text>PostEvent</Text>
        </View>
    );
};

export default PostEvent;